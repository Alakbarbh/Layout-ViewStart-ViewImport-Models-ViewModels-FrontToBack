@*@model List<Student>*@

@{
    //string[] names = (string[])ViewData["names"];
    //int[] nums = ViewBag.numbers;
}

@*@foreach (var item in nums)
{
    <h1>@item</h1>
}     *@

@*@foreach (var item in Model)
{
    <span>@item</span>
}     *@


@*@foreach (var item in Model)
{
    <h1>@item.Id</h1>
    <h1>@item.Fullname</h1>
    <h1>@item.Age</h1>
}*@


@*<span>@ViewBag.address</span>*@

@model HomeVM

<main>
    <ul>
        @foreach (var item in Model.Students)
       {
          <li>@item.Fullname</li>
          <li>@item.Age</li>
       }
    </ul>
    
    
    <h1>@Model.Product.Name</h1>
    <h1>@Model.Product.Price</h1>
</main>


